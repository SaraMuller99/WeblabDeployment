## Haciéndolo en python (recomendado para raspy)
.vscode -> settings.json : Se añade el path del experiento a ver si así funciona -> No parece que valga para nada, seguramente no tiene sentido

El códgio del dummy está en c++, está en experiments->manage->server->cpp->dummy_experiment_server

Añadir myexample donde está el dummy pero en la parte de python a ver si funciona, también se modifica el configuration.yml para ponerle el experiments. antes del nombre de la clase: Nada, esto no funciona, LO QUITO

Añadir myexample donde está el dummy pero en la parte de python a ver si funciona, sin modificar el configuration.yml para ponerle el experiments. antes del nombre de la clase: Nada, esto no funciona, LO QUITO

Añadir myexample en deployments a la altura de example, sin modificar el configuration.yml . : Nada, esto no funciona, LO QUITO

Nada de lo que he hecho ha funcionado, no entiendo el motivo por el cuál no funciona

## Haciéndolo en weblab python (no específicamente recomendado para rasppy)